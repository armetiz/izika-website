---
import TrackedLink from './TrackedLink.astro';

/**
 * Closing CTA band — carries over the landing-specific footer pitch of the
 * old solo/team templates (tracked as cta_footer), which the unified Footer
 * dropped.
 */
interface Props {
  title: string;
  text: string;
  ctaLabel: string;
  trackingContext: string;
}

const { title, text, ctaLabel, trackingContext } = Astro.props;
---

<section class="bg-beige-light">
  <div class="mx-auto max-w-4xl px-4 py-16 text-center">
    <h2 class="text-[2.5rem] font-bold">{title}</h2>
    <p class="mx-auto mt-3 max-w-2xl">{text}</p>
    <TrackedLink
      eventId="cta_footer"
      trackingContext={trackingContext}
      class="btn-3d mt-6 inline-block min-w-40 rounded-[0.3125rem] bg-primary px-8 py-3 text-center font-bold text-white hover:bg-primary-hover"
    >
      {ctaLabel}
    </TrackedLink>
  </div>
</section>
